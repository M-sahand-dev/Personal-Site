import React from 'react'

const Navigatio = ({ children }) => {
    return (
        <div className="flex justify-between items-center">
            {children}
        </div>
    );
}

export default Navigatio